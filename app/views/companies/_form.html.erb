<%= form_for @company do |f| %>
  <div class="form-group row">
    <label class="col-sm-2 col-form-label"><%= f.label :name %></label>
      <div class="col-sm-10">
        <%= f.text_field :name %>
      </div>
  </div>
  <%= f.submit %>
<% end %>
